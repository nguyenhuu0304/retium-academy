import {ImageResponse} from 'next/og';
import {verify} from '../../../../lib/certificate';
import {modules} from '../../../questions';
export const runtime='nodejs';
export async function GET(req:Request,{params}:{params:Promise<{token:string}>}){const {token}=await params;const c=verify(token);if(!c)return new Response('Certificate not found',{status:404});const mod=modules.find(m=>m.id===c.module)!;const logo=new URL('/retium-logo.png',req.url).toString();return new ImageResponse(<div style={{display:'flex',width:'100%',height:'100%',background:'#11141d',padding:26,color:'#fff',fontFamily:'sans-serif'}}><div style={{display:'flex',flexDirection:'column',alignItems:'center',justifyContent:'center',width:'100%',height:'100%',border:'3px solid #ff5b24',borderRadius:16,background:'#1b202b',gap:17}}><img src={logo} width={82} height={82} alt="Retium logo"/><div style={{fontSize:18,letterSpacing:6,color:'#ff9b64'}}>RETIUM ACADEMY</div><div style={{fontSize:48,fontWeight:800}}>Certificate of Knowledge</div><div style={{height:2,width:250,background:'#ff5b24'}}/><div style={{fontSize:19,color:'#d1d5db'}}>This certificate is presented to</div><div style={{fontSize:58,fontWeight:800,maxWidth:950,textAlign:'center',overflow:'hidden'}}>{c.name}</div><div style={{fontSize:28,color:'#ff8b50'}}>{mod.en}</div><div style={{fontSize:26}}>Score: {c.score} / {c.total}</div><div style={{fontSize:15,color:'#b5bfcc'}}>INDEPENDENT COMMUNITY CERTIFICATE · {c.issued.slice(0,10)}</div><div style={{fontSize:15,color:'#ff9b64',letterSpacing:4}}>LEARN · TEST · GROW</div></div></div>,{width:1200,height:630,headers:{'Cache-Control':'public, max-age=300'}})}
